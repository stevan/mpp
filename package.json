{
  "name": "mpp",
  "version": "0.1.0",
  "description": "Modern Perl Parser - A parser framework for imperative statement-based programming languages",
  "type": "module",
  "main": "js/index.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test js/tests/**/*.test.js",
    "test:watch": "node --test --watch js/tests/**/*.test.js"
  },
  "keywords": ["parser", "perl", "async", "generator", "pipeline"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
